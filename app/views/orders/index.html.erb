<%# @orders.each do |order| %>
<%# if session[:login] == 1 || order.user.email == current_user.email  %>
<div id="itemsContainer">
 
 <p id="notice"><%= notice %></p>
  
<h1>Orders</h1>
 
<table>
  <thead>
    <tr>
      <th>Order date</th>
      <th>User</th>
      <th>Status</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    

    <% @orders.each do |order| %>
    <%#@users.each  do |user| %>

     <% if session[:login] == 1 || order.user.email == current_user.email  %>
      <tr>
        <td><%= order.order_date %></td>
        <td><%= order.user.email%></td>
        <td><%= order.status %></td>
        
        <td><%= link_to 'Show', order %></td>
        
      
          <% if session[:login] == 1 %>   
        <td><%= link_to 'Edit', edit_order_path(order) %></td>
        <td><%= link_to 'Destroy', order, method: :delete, data: { confirm: 'Are you sure?' } %></td>
         </tr>
        <% end %>
     <%end %>
 <%end %>
  </tbody>
</table>

 
 <td><%= link_to 'Back', cart_path, :class =>"button" %></td>

 
 
 
 <p id="notice"><%= notice %></p>
 <%# @orders.each do |order| %>
<% if session[:login] == 1 %>

<h2>See All the Booked Classes</h2>

 <table id="customers">
   
   <tr>
 <td>Class Available date and hours:</td>
 <td>Class Name:</td>
 <td>Class Adress:</td>
 <td>Class price:</td>
 <td>Order Id:</td>
  <td>Order Quantity:</td>
  
 
 </tr>
      <tr>
        <% @orderclasses.each do |orderclass| %>
        <td><%= orderclass.hourAndDateAvailable %></td>
        <td><%= orderclass.name%></td>
        <td><%= orderclass.adress %></td>
        <td><%= orderclass.price %></td>
        <td><%= orderclass.order_id %></td>
        <td><%= orderclass.quantity %></td>
      </tr>
    <% end %>
    
<%end %>
<%#end %>
 
   <tr></tr>
   </table>
   </div>
   <br>

</div>




