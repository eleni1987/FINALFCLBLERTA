
<% if @cart.empty? %>
<h2 style="text-align: left; color:#4CAF50";>Your Cart is Empty</h2>
<h2 style="text-align: left; color:#4CAF50";>Book Your Classes Now by clicking Continue Shopping!!</h2>
<% end %>

<br>

<div id="itemsContainer">
    <table id="customers">
        <% total = 0 %>
        <tr style= "background-color:#4CAF50"><td>Class Look Like!!!</td><td>Fitness Class Name</td><td>Remove Class From Your Cart</td><td>Incraese Quantity of Class</td><td>Reduce Quantity of Class</td><td>Price</td><td>Quantity</td><td>Total</td></tr>
 <tr>
        <% @cart.each do |id, quantity| %> 
        
          <%fitness_class = FitnessClass.find_by_id(id) %>

   
 <tr>
 <td>
 <div class="image">
<class="image_url"><%= link_to   (image_tag fitness_class.image_url, :class => "itemholsIm") %>

 </div>

 </td>

 <td class="title"><%= link_to fitness_class.name, fitness_class %></td>
 <td><a href="#">Remove From Cart</a></td>
<td> <a href="cart/increase/<%= fitness_class.id %>" >Increase FitnessClasses<i class='fas fa-arrow-alt-circledown'></i></a></td>
 </td>
 <td> <a href="cart/reduce/<%= fitness_class.id %>" >Reduce FitnessClasses<i class='fas fa-arrow-alt-circledown'></i></a></td>
 </td>
 <td class="price"><%= number_to_currency(fitness_class.price, :unit =>"€") %></td>
 <td class="quantity"><%= quantity %></td>
 <td class="price"><%= number_to_currency(quantity * fitness_class.price, :unit =>"€") %></td>

 </tr>

 <% total += quantity * fitness_class.price %>
 <%end %>
 <tr>
 </tr>


    </table>
   <h2 style="text-align: left; background-color:#4CAF50";> Total Amount: <%= number_to_currency(total, :unit => "€") %> <h2>
    <a href="/fitness_classes" class="button">Continue Shopping</a>
    <a href="/cart/clear" class="button">Clear Cart</a>
    <a href="/checkout/" class="button">Checkout</a>
    <br><br>
    
</div>
